{
  "name": "portfolio-website",
  "version": "1.0.0",
  "scripts": {
    "start": "browser-sync start --server src --files 'src/**/*'"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "prettier": "^3.6.2"
  }
}
